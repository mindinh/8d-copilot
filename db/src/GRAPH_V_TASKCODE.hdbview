VIEW "GRAPH_V_TASKCODE" AS
SELECT
    CAST('TC:' || "TASKCODE" AS NVARCHAR(150)) AS "NODE_ID",
    CAST("TASKCODE" AS NVARCHAR(20)) AS "BIZ_KEY",
    CAST(MAX("TASKCODEGROUP") AS NVARCHAR(20)) AS "CODE_GROUP"
FROM "CNMA_PRORESOLVE_HISTORICALACTIONS"
WHERE "TASKCODE" IS NOT NULL
GROUP BY "TASKCODE"
