VIEW "GRAPH_V_FUNCTION" AS
SELECT DISTINCT
    CAST('FUN:' || "FUNCTIONTITLE" AS NVARCHAR(150)) AS "NODE_ID",
    CAST("FUNCTIONTITLE" AS NVARCHAR(100)) AS "BIZ_KEY"
FROM "CNMA_PRORESOLVE_HISTORICALTEAMMEMBERS"
WHERE "FUNCTIONTITLE" IS NOT NULL
