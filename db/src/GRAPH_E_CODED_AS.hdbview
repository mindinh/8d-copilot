VIEW "GRAPH_E_CODED_AS" AS
SELECT
    CAST('CODED_AS|' || "ID" AS NVARCHAR(150)) AS "EDGE_ID",
    CAST('ACT:' || "ID" AS NVARCHAR(150)) AS "SOURCE_ID",
    CAST('TC:' || "TASKCODE" AS NVARCHAR(150)) AS "TARGET_ID"
FROM "CNMA_PRORESOLVE_HISTORICALACTIONS"
WHERE "TASKCODE" IS NOT NULL
