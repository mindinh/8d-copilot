VIEW "GRAPH_V_ROOTCAUSE" AS
SELECT DISTINCT
    CAST('RC:' || "ROOTCAUSECATEGORY" AS NVARCHAR(150)) AS "NODE_ID",
    CAST("ROOTCAUSECATEGORY" AS NVARCHAR(30)) AS "BIZ_KEY"
FROM "CNMA_PRORESOLVE_HISTORICALCASES"
WHERE "ROOTCAUSECATEGORY" IS NOT NULL
